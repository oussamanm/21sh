


alt + > :: Select right charachter.

alt + < :: Select left charachter.

alt + c :: Copy.

alt + v :: Paste.

alt + x :: Cut.

home :: Move the cursor to the begining.

end :: Move the cursor to the end.

alt + (Up | Down) :: Moving by lines.

alt + (Q | W) :: Moving by words.


